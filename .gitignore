# Private working notes — must never reach the public repo
docs/
.serena/
.claude/
.playwright-mcp/
.mcp.json

# Per-instance eval sets contain real client data — never commit
evals/eval*.xml
evals/answers*.xml

node_modules/
dist/
*.log
.env
.env.*
vector-index.json
coverage/
.DS_Store
*.mcpb
.mcpb-build/
